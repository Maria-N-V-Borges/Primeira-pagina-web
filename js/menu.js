function toggleMenu() {
	const menu = document.getElementById("menuLinks");
	menu.classList.toggle("ativo");
}

// clasList.toggle("ativo") significa:
// se tiver ➡️ remove
// se não tiver ➡️ adiciona
